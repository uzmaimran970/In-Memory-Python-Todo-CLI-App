# T018: Audit Service
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-audit
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8003
      protocol: TCP
  selector:
    app: audit-svc
